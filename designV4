分析結果を確認しました。
以下の方針で確定します。

1. 用語は分離してください。

- inputTableName：
  受信系Flowにおける読み取り元・入力テーブル名。
  flow-input-table.yamlから取得する。

- targetTable：
  送信系Flow等における更新対象・書き込み先テーブル名。
  既存の意味・既存ロジックを維持する。

flow-input-table.yamlのinputTableをtargetTableとして扱わないでください。

2. 今回のflow-input-table.yamlの適用範囲は、
   recv-full / recv-diff の受信系Flowに限定してください。

YAML内に送信系Flowが存在していても、
今回の設計変更では使用しません。
送信系への適用は今回の対象外とします。

3. 受信系Flowの入力テーブル名解決は以下としてください。

- flow-input-table.yamlをFlow名で参照する
- 該当Flowが存在する場合：
  inputTableをinputTableNameとして確定する
- 該当Flowが存在しない場合：
  推測・補完せず、従来どおり確認要とする
- YAMLに登録されていないFlowについて、
  類似Flow名などから推測しない

4. R2900の場合は、
   R2900 → inputTableName=tm0010
   と解決され、
   tm0010にテストデータを準備してFlowを実行する
   Test Caseを生成できる設計としてください。

その場合、
「入力テーブル名が未確定」であることだけを理由とした
TC-CONFIRMは生成しないでください。

以上の方針でdesign.mdを修正してください。

requirements.md、tasks.md、steering、
flow-input-table.yamlは変更しないでください。

修正後、
- 変更したセクション
- inputTableNameの解決フロー
- R2900が最終的にどのようなTest Case生成につながるか

を簡潔に報告してください。

---------------------------------------------------------------------

design.mdの修正内容を確認しました。

次に、修正後のdesign.mdを使用した場合に、
R2900のTest Caseがどのように生成されるか確認したいです。

まだtasks.mdやrequirements.mdは変更しないでください。
実装も行わないでください。

R2900について、現在の
- requirements.md
- design.md
- flow-input-table.yaml
- R2900のlayout/flow入力情報

を使用して、Test Case仕様書のサンプルを再生成してください。

特に以下を確認してください。

1. inputTableNameがtm0010として解決されること
2. 前提条件に「tm0010へ具体的なテストデータを準備する」ことが記載されること
3. 具体的な入力項目と設定値が記載されること
4. 具体的な期待結果が記載されること
5. 入力テーブル名未確定だけを理由としたTC-CONFIRMが生成されないこと
6. 確定できない情報が他に存在する場合のみ、TC-CONFIRMとして分離されること

ファイルはまだ変更せず、生成結果だけを提示してください。
