今回、このExcelはTest Case生成に必要な
「入力テーブル名」を補完する目的だけで使用します。

対象範囲を広げず、まず以下だけを確認してください。

1. 各Flowを一意に識別できる項目
2. そのFlowに対応する入力テーブル名
3. 上記2項目だけで
   「Flow → 入力テーブル名」
   の対応表を作成できるか

その他の列（出力先、スキーマ、IF情報等）は、
今回の対象外としてください。

また、値を推測して補完しないでください。
対応関係が特定できないFlowがある場合は、
そのFlowを「未特定」として一覧化してください。

まだrequirements.md、design.md、tasks.md、
steeringは変更しないでください。

まず分析結果と、
最小限の対応表をどの形式で保持するのが適切かを提案してください。

---------------------------------------------------------------
interfaceList.xlsm について、対応ルールを確定しました。

- C列「データ種別」：Flow名
- P列「マスタHUBテーブル名」：入力テーブル名

したがって、
「C列のFlow名 → P列の入力テーブル名」
の対応を使用してください。

今回の目的は、Test Case生成時に不足している
入力テーブル名を補完することだけです。

まだ requirements.md、design.md、tasks.md、steering は変更しないでください。

まず、interfaceList の内容から
Flow名と入力テーブル名の対応一覧を作成できるか確認してください。

対応できないFlow、P列が空欄のFlow、重複しているFlowがあれば、
それぞれ一覧化してください。

値は推測して補完しないでください。
