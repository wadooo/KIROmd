今回、このExcelはTest Case生成に必要な
「入力テーブル名」を補完する目的だけで使用します。

対象範囲を広げず、まず以下だけを確認してください。

1. 各Flowを一意に識別できる項目
2. そのFlowに対応する入力テーブル名
3. 上記2項目だけで
   「Flow → 入力テーブル名」
   の対応表を作成できるか

その他の列（出力先、スキーマ、IF情報等）は、
今回の対象外としてください。

また、値を推測して補完しないでください。
対応関係が特定できないFlowがある場合は、
そのFlowを「未特定」として一覧化してください。

まだrequirements.md、design.md、tasks.md、
steeringは変更しないでください。

まず分析結果と、
最小限の対応表をどの形式で保持するのが適切かを提案してください。

---------------------------------------------------------------
interfaceList.xlsm について、対応ルールを確定しました。

- C列「データ種別」：Flow名
- P列「マスタHUBテーブル名」：入力テーブル名

したがって、
「C列のFlow名 → P列の入力テーブル名」
の対応を使用してください。

今回の目的は、Test Case生成時に不足している
入力テーブル名を補完することだけです。

まだ requirements.md、design.md、tasks.md、steering は変更しないでください。

まず、interfaceList の内容から
Flow名と入力テーブル名の対応一覧を作成できるか確認してください。

対応できないFlow、P列が空欄のFlow、重複しているFlowがあれば、
それぞれ一覧化してください。

値は推測して補完しないでください。
---------------------------------------------------------------

今回は、P列「マスタHUBテーブル名」に値が存在するものだけを対象にしてください。

対応ルール：
- C列「データ種別」＝ Flow名
- P列「マスタHUBテーブル名」＝ 入力テーブル名

以下の条件で対応表を作成してください。

1. P列に値が存在する行だけを抽出する
2. C列をFlow名、P列を入力テーブル名として保持する
3. P列が空欄の行は対象外とする
4. 値を推測・補完しない
5. 「種別」「製品」「二次加工構成品」等、物理テーブル名ではないと判断された値は含めない
6. 同一Flowに同一テーブル名が複数存在する場合は1件に集約する
7. 同一Flowに異なるテーブル名が存在する場合は自動決定せず、別途一覧化する

この条件で、Test Case生成時に参照する
Flow名 → 入力テーブル名
の最小限の対応表を作成してください。

まだ requirements.md、design.md、tasks.md、steering は変更しないでください。

まず対応表を作成し、作成件数と除外件数を報告してください。
------------------------------------------------------------------------
確認しました。

この31件を正式な
Flow名 → 入力テーブル名
の対応情報として使用します。

flow-input-table.yaml を作成してください。

内容は31件の対応関係だけを保持し、
P列空欄の312件および除外した14件は記載しないでください。

形式は以下のような単純なマップとしてください。

R2900:
  inputTable: tm0010

R2901:
  inputTable: tm0011

値の推測・補完は行わないでください。

また、元Excelのパスやsheet名、resolved/unresolved等の
解析用情報はflow-input-table.yamlには不要です。

今回はflow-input-table.yamlの作成だけを行ってください。
requirements.md、design.md、tasks.md、steeringはまだ変更しないでください。

作成後、31件存在することを確認してください。
tmp配下に今回の解析用として作成した一時ファイルがあれば削除してください。
